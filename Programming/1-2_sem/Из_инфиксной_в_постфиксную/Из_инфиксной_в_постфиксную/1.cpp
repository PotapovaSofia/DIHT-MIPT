char third[2] = "^";
case '^':
			push(a,c);
			break;
			case '^':
						if ((a->count)<2)
						{
							printf("Error");
							return 0;
						}
						p = a->head->key;
						deleten1(a);
						m = a->head->key;
						deleten1(a);
						push1(a,(pow(m,p)));
						break;	
						#include <math.h>